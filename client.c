#include <stdio.h>
#include <string.h>
#include <unistd.h>

#include <sys/types.h>
#include <sys/socket.h>
#include <arpa/inet.h>


#define BUFLEN 1024
#define MASSAGELEN 1024

int main(int argc, char **argv)
{

	int socket;

	char buf[BUFLEN];
	int addrlen

	const struct sockaddr *addr;

	char message[MASSAGELEN];
	char reply[MESSAGELEN];

	sockfd = socket(AF_INET, SOCKSTREAM, 0);
	if(sock < 0)
		printf("failed to create socket");


}


